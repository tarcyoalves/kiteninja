# HANDOFF — 2026-08-23

Continuação de [`HANDOFF.md`](./HANDOFF.md) e
[`HANDOFF-2026-08-19.md`](./HANDOFF-2026-08-19.md). Leia este primeiro: ele
descreve o estado mais recente.

## Onde o trabalho parou (para quem chega agora)

Entrega funcional desta rodada: **`ad0cbf5`** em **`main`** (produção sai de
`main`, não de `master` — empurrar só para `master` gera apenas Preview).

Deploy de `ad0cbf5`: **Ready** em produção em 24/08/2026, com alias
[`https://kiteninja.vercel.app`](https://kiteninja.vercel.app). O log remoto
confirma clone desse commit, migração **110/110**, TypeScript e build Next
verdes.

| Verificação em produção | Resultado |
|---|---|
| `GET /` | 200 |
| `GET /manifest.webmanifest` | 200 (`application/manifest+json`) |
| `GET /api/auth/me` sem sessão | `{"user":null}` (não estoura) |
| `GET /api/sessions` sem sessão | 401 `Não autenticado` (rota protegida, sem 500) |
| `POST /api/auth/login` com senha errada | 401 (era 500 — a causa do "deslogado") |
| `GET /api/sos/active` sem sessão | 401 |
| `GET /api/cron/sos-escalada` sem segredo | 503 (falha fechado, deliberado) |
| `/convite/<token-inválido>` | "Convite indisponível" (sem 500) |

**O que continua faltando não é código, é configuração de ambiente:**
`CRON_SECRET` e as chaves VAPID na Vercel. Sem elas a varredura periódica não
roda e o socorrista não recebe push — o alerta só aparece para quem já está com
o app aberto. Passo a passo em [`docs/OPERACAO-SOS.md`](./docs/OPERACAO-SOS.md).

Tudo abaixo foi verificado por execução. Onde não deu para verificar, está
escrito **não verificado** — sem exceção.

## Estado dos portões de qualidade

| Portão | Resultado |
|---|---|
| `npx tsc --noEmit` | limpo |
| `npx vitest run` | **672 passando / 38 arquivos** |
| `npm run build` | **110/110 migrações + Next verde (34 páginas)** |
| `npx tsx scripts/verify-sos.ts` | **53 checagens, 0 falhas** |
| `npx tsx scripts/verify-sql.ts` | **227 checagens, 0 falhas** |
| `npm run test:db` | **31 checagens Neon, 0 falhas; limpeza concluída** |

> **Windows corrigido:** `package.json` agora usa `&&` entre migração e build.
> O antigo `;` era interpretado como parte do nome do arquivo pelo shell local.
> `npm run build` é novamente o portão oficial em Windows e Vercel.

## O que foi feito nesta sessão

### 1. Login voltou (era migração faltando, não bug de sessão)

`/api/auth/me` consultava `emergency_contact_name` / `emergency_contact_phone`,
colunas que **não existiam no Neon** — a migração do SOS nunca havia sido
aplicada em produção. Todo carregamento de perfil estourava e o app deslogava.

Corrigido com `npm run migrate` (auditado antes: 25 `CREATE TABLE IF NOT
EXISTS`, 13 `ADD COLUMN IF NOT EXISTS`, zero comando destrutivo). Dados
preservados e conferidos: 6 usuários, 30 mensagens, 25 sessões válidas.

**Lição para o próximo agente:** ao ver "deslogado", verifique se o schema em
produção acompanha o código antes de investigar cookie ou sessão.

### 2. Botão SOS não atrapalha mais o chat

O `SosButton` era `fixed right-3 bottom:4.5rem` renderizado sobre **todas** as
abas — caía exatamente sobre o campo de enviar mensagem. Agora o SOS vive no
menu do avatar, como o dono pediu: sem botão flutuante sobre a conversa.

### 3. Teclado do iOS não deixa mais vão no rodapé

Lógica pura extraída para `lib/keyboardInset.ts` (11 testes). Usa
`max(innerHeight, clientHeight) - viewportHeight`, ignora diferenças ≤130px
(retração da barra de endereço) e descarta leituras corrompidas (>75% da tela).
Remedição em 350ms + reset no `onBlur` cobrem o recolhimento.

### 4. Escalada do SOS: o defeito mais grave, corrigido

**Era assim:** a escalada de raio morava dentro de `GET /api/sos/active`, cuja
consulta filtra `WHERE sa.user_id = eu OR sr.user_id = eu`. Só escalava SOS que
o usuário com o app aberto **já enxergava**.

Consequência: um pedido de socorro cujos vizinhos notificados estavam todos com
o app fechado — justamente quando ampliar o raio é vital — não era varrido por
ninguém e ficava **parado em 5 km para sempre**, sem erro e sem log. E o
desenho exigia que o próprio acidentado (celular no bolso, molhado) mantivesse
o app aberto para que o socorro se ampliasse.

**Agora:** motor único em `lib/sosEscalada.ts`, com duas portas:

- `varrerEscaladas()` — varredura **global**, sem filtro por usuário (cron);
- `escalarUmSos()` — um alerta (poll de `/api/sos/active`, mantido).

Idempotente por construção: o `UPDATE` é condicionado ao raio lido
(`AND radius_km = $raioLido`), então cron e poll simultâneos ampliam **uma vez
só**. Provado em `scripts/verify-sos.ts`:

```
ok   consulta antiga NÃO alcança o SOS sem vizinhos online (a falha original)
ok   varredura global alcança o SOS que ninguém vê
ok   cron e poll simultâneos escalam UMA vez só
ok   raio ampliou 5 -> 15 km exatamente uma vez
```

Rota nova: `/api/cron/sos-escalada`, agendada em `vercel.json`.

**Segurança:** exige `Authorization: Bearer $CRON_SECRET` e **recusa tudo com
503 se a variável não existir** (fail-closed). Um endpoint que dispara push em
massa não pode virar porta aberta por variável esquecida no deploy.

### 5. Guardas de autorização ficaram mais honestos

`lib/authz.test.ts` analisava o código **com comentários**, o que produziu dois
falsos resultados:

- `downwind/convite/[token]/entrar/route.ts` "passava" na exigência de sessão
  só porque um comentário **menciona** `requireUser()` — a rota nunca o chama.
  Ela é legitimamente pública (onboarding do convidado de 12h) e agora está
  declarada como tal, com justificativa;
- uma rota cobrava `WHERE id = ${...}` por causa de um `UPDATE` que existia
  apenas em prosa — o teste punia quem documentava.

Agora comentários são removidos antes da auditoria, e há dois testes novos:

- **rotas de máquina exigem segredo e falham fechado** — cron precisa validar
  `Authorization` *e* recusar sem `CRON_SECRET`;
- **exceções declaradas ainda correspondem ao código** — pega justificativa
  órfã. Já encontrou três de verdade (a escalada que saiu de `sos/active`, e
  duas purgas que migraram para `lib/downwindDb.ts`). Justificativa órfã é
  perigosa: deixa a porta pré-aberta para uma futura mutação de mesmo nome
  entrar sem revisão.

### 6. "Diagnóstico de tela" removido

`components/DiagTela.tsx` foi apagado e saiu do menu lateral. O próprio código
pedia isso ("some quando o bug morrer") — os bugs que ele media (tarja do
rodapé, vão do teclado) estão resolvidos. As medições do iPhone 16 Pro Max
seguem registradas no comentário de `app/layout.tsx`, que explica por que o
`apple-mobile-web-app-status-bar-style` **não pode** voltar para
`black-translucent`.

### 7. SOS sem GPS avisava NINGUÉM (o segundo silêncio)

O defeito mais grave desta rodada, da mesma família do item 4: falhava sem erro,
sem log, e com a tela dizendo que deu certo.

**Era assim:** a escolha de quem notificar era feita só por raio geográfico, o
que exige a coordenada do pedinte (`getCurrentPosition`, timeout de 3s). Quando
ela não vinha — celular molhado, permissão negada, GPS lento na água — a lista
de candidatos vinha **vazia**. O SOS era gravado no banco e **ninguém era
avisado**, enquanto o velejador via "pedido enviado" e esperava socorro.

Ironia cruel: o cenário em que o GPS falha é justamente o cenário de emergência
real (aparelho molhado, pressa, mão tremendo).

**Agora:** `lib/sosCandidates.ts` une duas fontes independentes — proximidade
(depende de GPS) e **companheiros de downwind `em_andamento`** (não depende de
GPS nem de distância). Regras, com a razão de cada uma, no arquivo e em
`docs/OPERACAO-SOS.md` seção 5. Em resumo: entram quem está `confirmado`/
`navegando` e **sempre** o `apoio_terra` (nunca navega, mas é quem costuma
acionar a autoridade); ficam fora quem `desistiu` e todo downwind só `aberto`.

O socorrista passou a receber **por que** foi chamado (`motivo` em
`sos_responders`). Isso muda a decisão dele: um parceiro de downwind a 30 km pode
ser o socorro mais rápido, um vizinho a 30 km é irrelevante.

Provado em `scripts/verify-sos.ts` seção 6 (9 checagens novas), incluindo
"downwind encerrado para de notificar" e "sem GPS, distância vem nula em vez de
inventada".

### 8. Dois bugs de contrato: dado que o SELECT nunca trouxe

Invisíveis em teste unitário, porque viviam na diferença entre a consulta e o
tipo declarado em `SosAlertData`:

- **Socorrista sem nome e sem distância.** `/api/sos/active` lia
  `sos_responders` sem `JOIN users` e sem `distance_km`. O painel do acidentado
  listava quem estava vindo com nome vazio, e nunca mostrava a distância — a
  informação que decide quem chega primeiro.
- **"Posição não confirmada" em todo alerta.** A consulta não trazia
  `accuracy_m` nem o nome do spot, e a resposta não montava `temCoordenada`.
  Chegando `undefined`, o `SosIncomingAlert` dizia "Posição não confirmada"
  **sempre** — inclusive com GPS preciso — e escondia o botão "Ver no mapa"
  justamente quando havia mapa para ver.

Corrigidos com `LEFT JOIN spots`, `accuracy_m` no SELECT e os campos montados na
resposta. Travados por `verify-sos.ts` seção 7.

### 9. Autoridades saíram de trás do nosso fluxo

193/185 estavam **copiados em cinco arquivos** e apareciam **somente quando o
POST do SOS falhava**. Dois problemas: um número errado numa das cópias só se
revelaria numa emergência real; e o app escondia a autoridade (que tem barco e
mandato) atrás de um fluxo nosso que pode falhar.

Agora `lib/emergencia.ts` é fonte única (193, 185, 192, 190 + mensagem de
WhatsApp), `components/BotoesEmergencia.tsx` desenha, e os botões vivem **sempre
visíveis** no menu em "Segurança & Emergência". `tel:` funciona sem internet,
basta sinal de voz. Quando o POST falha, o texto diz explicitamente que a
comunidade **não** foi avisada (`TEXTO_FALHA_REDE`) — meia verdade aqui custa
vida. Coberto por `lib/emergencia.test.ts`.

### 10. Varredura de navegação, Logbook, Comunidade e pipeline (23/08 à noite)

- Menu lateral deixou de ter destinos duplicados: `Eventos & Ocorrências` é um
  único item; `Riders` abre a busca de pessoas; `Meu Logbook` chega à tela de
  sessões; `Notificações` abre a central própria.
- Registrar Ride e publicar relato agora aguardam o servidor, impedem duplo
  toque, exibem falha e não apagam o formulário. Antes a UI fechava mesmo se a
  API recusasse. No Logbook, falha preserva os dados para retentativa; sucesso
  limpa o formulário para a próxima sessão.
- O Logbook enviava `dd/mm/aaaa` para uma coluna PostgreSQL `DATE`. Agora o
  cliente envia ISO e a API valida data civil/hora; data impossível retorna 400,
  não 500. Métricas opcionais iguais a zero são preservadas na gravação e na
  resposta, e rajada vazia não vira zero. Modelo da prancha e privacidade do
  Ride passaram a ter controles visíveis no formulário.
- Ride público + post automático passaram para uma única CTE. Falha no post não
  deixa uma sessão parcialmente gravada para duplicar na retentativa.
- Encontrado defeito crítico no deploy: `migrate-on-build.ts` dividia SQL por
  todo `;`, quebrando os dois blocos `DO $$` do SOS em dez instruções inválidas,
  mas seguia o build e imprimia “concluído”. Há agora um scanner compartilhado
  (`lib/splitSqlStatements.ts`) com testes para strings, comentários, blocos e
  dollar-quotes. `migrate.ts`, build e `verify-sql.ts` usam o mesmo caminho.
- O Neon foi auditado antes da aplicação: índice único e CHECK do SOS presentes,
  zero SOS abertos duplicados e zero raios inválidos. Migração idempotente então
  executada online: **110/110**, sem perda de dados.
- O build usa `&&` (Windows + Linux) e falha se uma migração realmente falhar
  quando a URL do banco está disponível; prefere `DATABASE_URL_UNPOOLED`.

Documentação detalhada e resultados em [`PROGRESS.md`](./PROGRESS.md).

## Pendências que dependem de você (dono), não de código

Detalhes em [`docs/OPERACAO-SOS.md`](./docs/OPERACAO-SOS.md).

1. **`CRON_SECRET` na Vercel** — sem ela a varredura responde 503 e não roda.
2. **Chaves VAPID** — **não verificado** se existem em produção. Sem elas o
   socorrista não recebe aviso: o SOS fica no banco, mas o push não sai.
3. **Limite do plano Hobby** — a Vercel só permite **cron diário**, o que é
   quase inútil para uma emergência de minutos. Hoje a escalada confiável ainda
   depende do caminho preguiçoso (alguém com o app aberto). Para escalada em
   minutos: plano Pro (`* * * * *`) ou acionador externo.

## O que NÃO foi provado

- fluxo SOS ponta a ponta **com dois aparelhos reais**;
- entrega de push em iPhone com PWA instalado;
- a varredura por cron nunca executou (falta `CRON_SECRET`);
- comportamento do teclado no iPhone real — o navegador de preview reporta
  `innerHeight`/`clientHeight` como 0, então só os testes unitários garantem a
  lógica.

## Armadilhas do repositório

- **`main` e `master` coexistem.** Produção sai de **`main`**; push só em
  `master` gera apenas Preview. Confira antes de anunciar deploy.
- Driver Neon aceita **somente** template tag: `sql\`...\``. A forma
  `sql(texto)` lança erro.
- CSS de produção fica em `/_next/static/immutable/chunks/`, não em
  `/_next/static/css/`.
- Nunca cole segredos (`DATABASE_URL`, `CRON_SECRET`, VAPID) em chat ou log.
